# Build-Konfiguration. Wird vor dem PyInstaller-Lauf überschrieben,
# um die fest eingebaute Default-Sprache zu setzen. Im normalen Python-Modus
# (run.bat/.sh) bleibt der Default "auto" — d.h. System-Locale entscheidet.
BUILD_DEFAULT_LANG = "auto"
